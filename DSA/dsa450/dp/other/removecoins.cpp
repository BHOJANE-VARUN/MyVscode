#include <bits/stdc++.h> 
using namespace std;
int solve(vector<int> &coins,int turn,int doc,int me,int i)
{
    if()
}
int maxCoins(vector<int> coins, int numberOfCoins)
{
    // Write your code here
    if(coins.size()<3)
    {
        return coins[max_element(coins.begin(),coins.end())-coins.begin()];
    }  
}