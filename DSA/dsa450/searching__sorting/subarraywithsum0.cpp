#include <bits/stdc++.h> 
using namespace std;
int countSubarrays(int n, vector<int> &arr){
    for(int i=0;i<n-1;i++)
    {
        int ele = -arr[i];
        int low= i+1,high = n-1;
        while (high>low)
        {
            if(arr[mid]==ele)count++,
        }
        
    }
}